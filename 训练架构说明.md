# MemAgent 强化学习训练架构详细说明

## 一、整体架构概览

当前 `run1k.sh` 脚本实现了一个基于 **PPO (Proximal Policy Optimization)** 的强化学习训练系统，专门用于训练具有**记忆能力**的智能体（Memory Agent）。

### 1.1 核心组件

```
训练流程：
┌─────────────────────────────────────────────────────────────┐
│                    main_ppo.py (入口)                        │
│  - 初始化 Ray 集群                                           │
│  - 创建 TaskRunner (Ray Actor)                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              RayPPOTrainer (ray_trainer.py)                 │
│  - 管理训练循环 (fit)                                        │
│  - 协调各个 Worker                                           │
│  - 处理数据加载与验证                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
┌─────────────┐ ┌──────────┐ ┌──────────────┐
│ Actor Worker│ │ Critic   │ │ Reward Model │
│ (FSDP)      │ │ Worker   │ │ Worker       │
└─────────────┘ └──────────┘ └──────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│            Recurrent Memory Agent (核心)                     │
│  - MemoryAgent (文本记忆) 或 KVCacheMemoryAgent (KV记忆)     │
│  - 多轮对话管理                                              │
│  - 记忆更新机制                                              │
└─────────────────────────────────────────────────────────────┘
```

## 二、当前实现：文本记忆架构

### 2.1 数据流

#### 2.1.1 数据加载阶段

**文件位置**: `recurrent/impls/memory.py` → `MemoryDataset`

```python
# 数据预处理流程
1. 从 parquet 文件加载数据
2. 提取 context (长文本) 和 prompt (问题)
3. 将 context 按 chunk_size 分块存储
4. 返回: context_ids, context_length, prompt_ids
```

**关键配置** (`run1k.sh`):
- `recurrent.enable=memory`: 启用记忆机制
- `recurrent.memory.config.chunk_size=5000`: 每个记忆块大小（token数）
- `data.context_key='context'`: 数据中context字段名
- `data.max_prompt_length=4096`: 最大prompt长度

#### 2.1.2 训练循环中的记忆处理

**文件位置**: `recurrent/impls/memory.py` → `MemoryAgent`

**核心方法**:

1. **`start()`**: 初始化记忆状态
   ```python
   self.memory = np.empty(self.bsz, dtype=object)  # 存储文本记忆
   self.step = 0  # 当前处理到第几个chunk
   ```

2. **`action()`**: 构造每轮的输入
   - **非最终轮**: 构造包含 `prompt + memory + chunk` 的模板
   - **最终轮**: 构造包含 `prompt + memory` 的最终回答模板
   - 返回: `messages` (token ids列表) 和 `meta_info` (生成参数)

3. **`update()`**: 更新记忆
   ```python
   # 将模型生成的文本响应存储为记忆
   self.memory[self.active_mask] = unpad(
       self.tokenizer, 
       gen_output.batch['responses'], 
       remove_eos=True
   )
   ```

4. **`done()`**: 判断是否结束（所有chunk处理完）

### 2.2 训练流程详解

#### 阶段1: Rollout (生成阶段)

**文件位置**: `verl/trainer/ppo/ray_trainer.py` → `_rollout()`

```python
# 伪代码流程
for epoch in range(total_epochs):
    for batch in train_dataloader:
        # 1. 初始化记忆Agent
        generation_manager.run_llm_loop(gen_batch, timing_raw)
        
        # 2. 多轮生成循环
        while not agent.done():
            # 2.1 Agent构造输入 (包含记忆文本)
            messages, meta_info = agent.action()
            
            # 2.2 调用模型生成
            gen_output = actor_rollout_wg.generate_sequences(messages)
            
            # 2.3 更新记忆 (文本形式)
            agent.update(gen_output)
        
        # 3. 获取最终输出
        final_mask, sample_index = agent.end()
```

**关键点**:
- 每轮都会将**记忆作为文本**拼接到输入中
- 模型需要重新编码整个 `prompt + memory + chunk` 序列
- 记忆以**文本token序列**形式存储和传递

#### 阶段2: 奖励计算

**文件位置**: `verl/trainer/ppo/ray_trainer.py` → `_compute_reward()`

```python
# 使用最终轮次的输出计算奖励
result = self.reward_fn(final_batch, return_dict=True)
reward_tensor = result["reward_tensor"]
```

#### 阶段3: PPO更新

**文件位置**: `verl/trainer/ppo/ray_trainer.py` → `_update_actor()`, `_update_critic()`

```python
# 1. 计算优势函数
advantages = compute_advantages(...)

# 2. 更新Actor (策略网络)
actor_loss = compute_ppo_loss(...)
actor_optimizer.step()

# 3. 更新Critic (价值网络)
critic_loss = compute_critic_loss(...)
critic_optimizer.step()
```

## 三、KV Cache 记忆架构（目标实现）

### 3.1 已存在的KV Cache实现

**文件位置**: `recurrent/impls/kvcache_memory.py` → `KVCacheMemoryAgent`

### 3.2 核心差异对比

| 特性 | 文本记忆 (MemoryAgent) | KV Cache记忆 (KVCacheMemoryAgent) |
|------|----------------------|--------------------------------|
| **记忆存储** | 文本token序列 | past_key_values (KV cache) |
| **编码方式** | 每轮重新编码整个序列 | 只编码新chunk，复用KV cache |
| **内存效率** | 低（重复编码） | 高（缓存复用） |
| **计算效率** | 低（重复计算） | 高（增量计算） |
| **实现复杂度** | 简单 | 复杂（需管理KV cache） |

### 3.3 KV Cache实现要点

#### 3.3.1 两阶段处理

```python
# 阶段1: Prefill (处理chunks，累积KV cache)
for chunk in chunks:
    # 只对当前chunk进行prefill
    past_key_values = model(input_ids=chunk, use_cache=True).past_key_values
    # 累积KV cache
    kv_cache = concat_past_kv(kv_cache, past_key_values)

# 阶段2: Decode (使用累积的KV cache生成最终答案)
final_output = model(
    input_ids=final_prompt,
    past_key_values=kv_cache,  # 复用所有chunk的KV cache
    use_cache=True
)
```

#### 3.3.2 关键工具函数

**文件位置**: `recurrent/kvcache_utils.py`

- `concat_past_kv()`: 合并多个KV cache
- `truncate_past_kv()`: 截断过长的KV cache
- `kv_seq_len()`: 获取KV cache序列长度

#### 3.3.3 Rollout支持

**文件位置**: `verl/workers/rollout/naive/naive_rollout_kv.py` → `NaiveKVRollout`

```python
# 支持prefill和decode两种模式
def generate_sequences(self, prompts: DataProto, ...):
    stage = prompts.meta_info.get("stage", "decode")
    if stage == "prefill":
        return self._prefill(prompts)  # 返回past_key_values
    return self._decode(prompts)  # 使用past_key_values
```

## 四、修改指南：从文本记忆迁移到KV Cache

### 4.1 脚本配置修改

在 `run1k.sh` 中需要修改：

```bash
# 当前配置（文本记忆）
recurrent.enable=memory \
recurrent.memory.config.chunk_size=5000 \

# 修改为（KV Cache记忆）
recurrent.enable=memory \
recurrent.memory.path=/home/admin123/dl/MemAgent/recurrent/impls/kvcache_memory.py \
recurrent.memory.name=REGISTER \
recurrent.memory.config.chunk_size=1024 \
recurrent.memory.config.max_prompt_length=${MAXLEN} \
recurrent.memory.config.max_memorization_length=${MAX_NEW_TOKEN} \
recurrent.memory.config.max_final_response_length=${MAX_NEW_TOKEN} \
recurrent.memory.config.max_chunks=1 \
+recurrent.memory.config.prompt_as_first_chunk=False \
recurrent.memory.config.kv_cache_max_length=4096 \
recurrent.memory.config.kv_cache_dtype=float16 \
recurrent.memory.config.reuse_prefill=True \
```

### 4.2 Rollout后端修改

需要确保使用支持KV cache的rollout：

```bash
# 当前可能使用
actor_rollout_ref.rollout.name=hf \

# 需要改为支持KV的rollout
# 检查 verl/workers/fsdp_workers.py 中的 _build_rollout 方法
# 可能需要使用 NaiveKVRollout 或类似的实现
```

### 4.3 关键代码位置总结

#### 需要理解的核心文件：

1. **记忆Agent实现**:
   - `recurrent/impls/memory.py`: 文本记忆实现
   - `recurrent/impls/kvcache_memory.py`: KV cache记忆实现

2. **训练循环**:
   - `verl/trainer/ppo/ray_trainer.py`: 主训练器，包含rollout和更新逻辑
   - `recurrent/generation_manager.py`: 同步生成管理器
   - `recurrent/async_generation_manager.py`: 异步生成管理器

3. **Rollout实现**:
   - `verl/workers/rollout/naive/naive_rollout.py`: 基础rollout
   - `verl/workers/rollout/naive/naive_rollout_kv.py`: 支持KV cache的rollout
   - `verl/workers/fsdp_workers.py`: FSDP worker，负责构建rollout

4. **数据加载**:
   - `recurrent/impls/memory.py` → `MemoryDataset`: 数据预处理
   - `verl/trainer/ppo/ray_trainer.py` → `_create_dataloader()`: 创建数据加载器

5. **接口定义**:
   - `recurrent/interface.py`: RAgent接口定义，所有记忆Agent需要实现

### 4.4 数据流对比

#### 文本记忆流程：
```
Batch → MemoryAgent.start()
  → action() [构造: prompt + memory_text + chunk]
  → model.generate() [编码整个序列]
  → update() [存储生成的文本为memory]
  → action() [下一轮，再次编码memory_text]
  → ... (重复)
```

#### KV Cache流程：
```
Batch → KVCacheMemoryAgent.start()
  → action() [stage="prefill", 只输入chunk]
  → model.prefill() [返回past_key_values]
  → update() [累积KV cache]
  → action() [下一个chunk的prefill]
  → ... (处理所有chunks)
  → action() [stage="decode", 输入final_prompt + past_key_values]
  → model.decode() [复用所有KV cache]
  → done()
```

## 五、关键配置参数说明

### 5.1 记忆相关配置

- `chunk_size`: 每个记忆块的大小（token数）
- `max_chunks`: 最多处理多少个chunk
- `max_memorization_length`: 记忆生成的最大长度
- `max_final_response_length`: 最终回答的最大长度
- `context_key`: 数据中context字段的名称

### 5.2 KV Cache特定配置

- `kv_cache_max_length`: KV cache的最大长度限制
- `kv_cache_dtype`: KV cache的数据类型（节省内存）
- `reuse_prefill`: 是否复用prefill阶段的KV cache
- `prompt_as_first_chunk`: 是否将prompt作为第一个chunk处理

### 5.3 训练相关配置

- `algorithm.adv_estimator=grpo`: 使用GRPO算法
- `actor_rollout_ref.actor.ppo_mini_batch_size=16`: PPO mini batch大小
- `actor_rollout_ref.actor.use_kl_loss=True`: 使用KL散度损失
- `trainer.total_epochs=10`: 训练轮数

## 六、调试建议

1. **检查记忆是否正确累积**: 在 `MemoryAgent.update()` 或 `KVCacheMemoryAgent.update()` 中添加日志
2. **验证KV cache形状**: 检查 `past_key_values` 的维度是否正确
3. **监控内存使用**: KV cache会占用额外GPU内存，注意 `kv_cache_max_length` 的设置
4. **检查rollout模式**: 确认使用的是支持KV cache的rollout实现

## 七、参考示例

可以参考 `run_memory_7B_kv.sh` 脚本，它已经配置了KV cache记忆模式。

